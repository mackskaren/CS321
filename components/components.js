const {buttons} = require('./buttons.js');
const {choices} = require('./hobbies.js');
const {weekdays, weekend} = require('./schedule.js');
const { modal } = require('./getzip.js');

module.exports = {
    buttons,
    choices,
    weekdays,
    weekend,
    modal
}